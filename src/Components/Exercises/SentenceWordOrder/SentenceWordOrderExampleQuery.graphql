query sentenceWordOrderExample($input: SentenceWordOrderExampleInputArgs!){
    sentenceWordOrderExample(input: $input) {
        id
        question
        correctAnswers
        level
        key
    }
}
